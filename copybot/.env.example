# ── Wallet ──────────────────────────────────────────────────
# Private key (hex, with or without 0x prefix) for your trading wallet.
# Fund this wallet on POLYGON with USDC for trading and MATIC for gas.
PRIVATE_KEY=

# Polygon RPC (for on-chain approvals & balance checks)
POLYGON_RPC_URL=https://polygon-rpc.com

# ── Target Trader ───────────────────────────────────────────
# Polymarket username to copy (default: anoin123)
TARGET_USERNAME=anoin123
# OR set the wallet address directly (skips username resolution)
# TARGET_ADDRESS=0x...

# ── Copy Settings ───────────────────────────────────────────
# Max USDC to spend per copied position (default: 10)
MAX_TRADE_USDC=10
# How often to poll for new positions, in seconds (default: 30)
POLL_INTERVAL_S=30

# ── SMS Notifications (Twilio) ──────────────────────────────
# Get these from https://console.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+1XXXXXXXXXX
# Your phone number for profit notifications
NOTIFY_PHONE=+19189066963
