# ── Wallet ──────────────────────────────────────────────────
PRIVATE_KEY=
POLYGON_RPC_URL=https://polygon-rpc.com

# ── Strategy ────────────────────────────────────────────────
# Max USDC per individual trade (default: 10)
MAX_TRADE_USDC=10
# Max total portfolio exposure (default: 100)
MAX_PORTFOLIO_USDC=100
# Max simultaneous positions (default: 10)
MAX_POSITIONS=10
# Scan interval in seconds (default: 60)
SCAN_INTERVAL_S=60
# Minimum edge to enter (default: 0.08 = 8%)
MIN_EDGE=0.08
# Minimum liquidity in market (default: 1000)
MIN_LIQUIDITY=1000
